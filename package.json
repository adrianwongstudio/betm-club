{
  "name": "betm-website",
  "version": "1.0.0",
  "description": "Marketing site for the Burnaby Entrepreneurs Toastmasters Club (Metrotown, Burnaby BC).",
  "private": true,
  "license": "MIT",
  "type": "commonjs",
  "scripts": {
    "start": "eleventy --serve",
    "build": "eleventy",
    "clean": "rm -rf _site",
    "postinstall": "git config core.hooksPath .githooks 2>/dev/null || true"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0"
  }
}
